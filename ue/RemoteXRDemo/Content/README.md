# Content policy

The first RemoteXR validation scene is generated entirely by C++ at runtime. No hand-authored map, Blueprint, material, media, or panorama asset is required.

Keep this directory for future optional production assets only. The baseline demo must continue to launch from the engine Entry map and remain reproducible from source.
